<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Needed so the Flutter tool can talk to the running app during debug. -->
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
